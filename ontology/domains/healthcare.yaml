ontology:
  id: saxon-healthcare
  name: Saxon Healthcare Ontology
  version: "0.1.0"
  description: >
    Domain extension for the Saxon Enterprise Core ontology.
  namespace: saxon.domain.healthcare
  extends: saxon.core
  extension_policy: additive

entities: {}
# Add domain concepts here. Every domain entity should extend a core concept.
#
# Example:
# DomainEntity:
#   extends: Asset
#   description: Domain-specific specialization.

relationships: {}
# Add only relationships whose meaning is genuinely domain-specific.
#
# Example:
# healthcare_RELATIONSHIP:
#   source: DomainEntity
#   target: Entity

event_types: {}
fact_types: {}
aliases: {}
