neo4j
pydantic
pydantic-settings
python-dotenv
pyyaml
pytest
fastapi
uvicorn
httpx
# Just the service-account auth/token piece -- Drive API calls themselves go
# through httpx (already a dependency) rather than the much heavier
# google-api-python-client, to keep this connector's footprint small. The
# [requests] extra is needed by google.auth.transport.requests, which is
# what actually performs the token refresh call.
google-auth[requests]
# Text extraction for the Google Drive connector's PDF/Word support --
# pypdf/python-docx read whatever text layer the file already has (a
# scanned/image-only PDF has none, and is silently skipped rather than OCR'd
# -- OCR is real, separate scope, not something to fold in here).
pypdf
python-docx
# Runs periodic connector syncs in-process -- see app/graph/connector_scheduler.py.
apscheduler
# Encrypts OAuth refresh tokens at rest for the "google_drive_oauth"
# connector type (the one-click "Connect Google Drive" button) -- see
# app/graph/token_crypto.py.
cryptography
# Official MCP Python SDK -- app/mcp/server.py exposes the context graph as
# MCP tools, mounted into this same FastAPI app/process. Pinned to the 2.x
# line (FastMCP was renamed to MCPServer in 2.0 -- see
# mcp.server.mcpserver.MCPServer, not mcp.server.fastmcp).
mcp>=2.0.0
graphiti-core[anthropic,google-genai]>=0.29.3
# Pinned below 1.0 -- graphiti_core's bundled AnthropicClient (llm_client/anthropic_client.py)
# calls messages.create(..., temperature=...) in a shape that breaks under anthropic 1.x's
# API changes ("AsyncMessages.create() got an unexpected keyword argument 'temperature'").
# 0.121.0 is confirmed working against the installed graphiti-core version.
anthropic<1.0.0
